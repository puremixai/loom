{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Loom desktop capabilities — minimum set. The webview only talks to the sidecar over HTTP, so no frontend-initiated Tauri IPC is needed; plugin defaults cover the rare cases where plugin internals check capabilities.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default",
    "log:default"
  ]
}
